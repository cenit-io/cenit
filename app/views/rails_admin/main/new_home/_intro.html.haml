%section#open_ipass
  .container
    .row
      .col-sm-4
        .text-center
          %span.rounded.first
            = 'Free'
        .description
          %h1
            ='Be in Community'
          %p
            = 'Cenit IO is <strong>Open Source</strong> and <strong>Free</strong>,'.html_safe
            = 'and the right place to shared your integrations, launch your own server and also use pre-built integrations for many populars cloud applications.'
          .extra
            %h4.text-muted
              - if current_user
                - users = tenant_users
              - else
                - users = recent_users
            %ul.list-inline.list-unstyled
              - users.each do |u|
                - url = u.picture_url(50)
                - alt = (u.email.split('@'))[0]
                - title = u.name.present? ? u.name : alt
                %li
                  = image_tag url, alt: alt, title: title, class: 'img-circle', height: 50, width: 50
            .community-info
              - if (false || ENV['SHOW_SLACK_BADGE'])
                = link_to 'Join us!', (ENV['SLACK_INVITATION'] || 'https://cenit-slack.herokuapp.com')
                .frame
                  %iframe{ frameborder: "0", height: "20", src: "https://cenit-slack.herokuapp.com/badge.svg", width: "100", :style => 'margin-left: 5px' }
              - else
                .thanks
                  %span
                    = 'Thank you for'
                    = link_to (ENV['SLACK_INVITATION'] || 'https://cenit-slack.herokuapp.com') do
                      %i.fa.fa-slack
                      =' joining us!'
                    %sup
                      = t('admin.actions.dashboard.recent_users.more', count: (User.all.count/5 * 5))
      .col-sm-4
        .text-center
          %span.rounded.second.active
            ='iPaaS'
        .description
          %h1
            ='Integration Platform Services'
          %p
            ='Cenit IO is the easiest and most open iPaaS platform to '
            %strong
              ='connect'
            ='your applications,'
            %strong
              ='synchronize data'
            ='and'
            %strong
              ='automate'
            ='processes. Intuitive wizards and tools make it fast and easy.'
          .extra
            %ul.list-inline.list-unstyled.steps
              - apps = [{name: 'Data',icon: 'fa-cube'},
              {name: 'Events',icon: 'fa-bolt'},
              {name: 'Transformations',icon: 'fa-random'},
              {name: 'Connections',icon: 'fa-plug'}]
              - apps.each do |a|
                - icon = a[:icon]
                - title = a[:name]
                %li
                  %i{class: "fa fa-2x #{icon}", title: title}
            .community-info
              .thanks
                %span
                  = link_to '/cross_shared_collection', class: 'orange' do
                    ='Integrate anything to anything!'
      .col-sm-4
        .text-center
          %span.rounded.third
            ='400+'
        .description
          %h1
            ='Hundreds of Integrations'
          %p
            = 'Cenit IO has hundreds of integrations (<strong>shared collections</strong>) created by our community, that covers almost every popular APIs and is a great tool to create your own integration from scratch.'.html_safe
          .extra
            %ul.list-inline.list-unstyled
              - apps = [{name: 'Gmail',src: 'google_gmail.png'},
              {name: 'Magento',src: 'magento.png'},
              {name: 'Slack',src: 'slack.png'},
              {name: 'Trello',src: 'trello.png'}]
              - apps.each do |a|
                - url = a[:src]
                - alt = a[:name]
                - title = a[:name]
                %li
                  = image_tag "home/integrations/examples/#{url}", alt: alt, title: title, height: 50, width: 50
            .community-info
              .thanks
                %span
                  = link_to '/cross_shared_collection' do
                    ='Shared Collections'
                  = ' &amp; '.html_safe
                  = link_to '/open_api_directory' do
                    ='Open Api Directory'
                    %sup{title:'API specs supplied by APIs.guru'}='+ 470'