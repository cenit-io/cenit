%table.table.table-condensed.table-striped
  %thead
    %tr
      %th{class: "header pjax"}= ' '
      %th{class: "header pjax"}= 'Namespace'
      %th{class: "header pjax"}= 'Name'
      %th{class: "header pjax"}= 'Segment'
      %th{class: "header pjax"}= 'Triggers'
      %th{class: "header pjax"}= 'Updated at'
  %tbody
    - @filters.each do |filter|
      %tr{class: "#{@abstract_model.param_key}_row"}
        %td= ' '
        %td= filter.namespace
        %td
          - params = { model_name: @model_name }
          %a{href: index_path(params), class: 'pjax'}= filter.name
        %td
          - params = { model_name: @model_name, f: filter.segment }
          %a{href: index_path(params), class: 'pjax'}= filter.custom_title
        %td= filter.triggers
        %td= filter.updated_at
