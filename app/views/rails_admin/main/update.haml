= rails_admin_form_for @object, url: update_path(params.merge(bulk_ids: @bulk_ids)), html: { multipart: true, class: "form-horizontal denser", data: { title: 'Update'} } do |form|
  = form.generate nested_in: true

  .form-actions
    %button.btn.btn-primary{type: 'submit', name: '_save', :'data-disable-with' => t('admin.actions.update.submit')}
      %i.icon-edit
      = t('admin.actions.update.submit')
    %span.extra_buttons
      %button.btn{type: 'submit', name: '_continue', :'data-disable-with' => t('admin.form.cancel')}
        %i.icon-remove
        = t('admin.form.cancel')
