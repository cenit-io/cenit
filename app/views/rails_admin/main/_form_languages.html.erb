<%= form.select field.method_name, field.enum, { include_blank: true }.reverse_merge({ selected: field.form_value }),
                field.html_attributes.reverse_merge({ data: { enumeration: true }, placeholder: t('admin.misc.search') }) %>

<script type="text/javascript">
  $(document).ready(function () {
    $('#setup_stored_procedure_language').on('change', function (e) {
      var value = $(this).val(),
          help = $('#' + value + '-HELP');

      $('#setup_stored_procedure_code_field').prop('title', help.length ? help.html() : '');
    });

    $('#setup_stored_procedure_language').change();
  });
</script>

<script id="NodeJS-HELP" type="text/plain">
 - Use params object to access to stored procedure parameters.
   Ex: var c = params.a + params.b

 - Use done(result) to complete process and return data to
   Cenit-IO controller.
 - Use raise(error) to complete process and return error to
   Cenit-IO controller.
</script>
<script id="Ruby-HELP" type="text/plain">
 - Use params hash to access to stored procedure parameters.
   Ex: var c = params[:a] + params[:b]

 - Use done(result) to complete process and return data to
   Cenit-IO controller.
 - Use raise(error) to complete process and return error to
   Cenit-IO controller.
</script>
<script id="PHP-HELP" type="text/plain">
 - Use params hash to access to stored procedure parameters.
   Ex: var c = params['a'] + params['b']

 - Use done(result) to complete process and return data to
   Cenit-IO controller.
 - Use raise(error) to complete process and return error to
   Cenit-IO controller.
</script>
<script id="Python-HELP" type="text/plain">
 - Use params hash to access to stored procedure parameters.
   Ex: var c = params['a'] + params['b']

 - Use done(result) to complete process and return data to
   Cenit-IO controller.
 - Use raise(error) to complete process and return error to
   Cenit-IO controller.
</script>